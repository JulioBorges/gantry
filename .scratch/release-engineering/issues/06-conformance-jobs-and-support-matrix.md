# Conformance jobs and the published support matrix

Type: issue
Status: ready-for-agent
Slice: release-engineering#06
Spec: [`../spec.md`](../spec.md) (spec 19)
Created: 2026-09-12

## Parent

[`.scratch/release-engineering/spec.md`](../spec.md)

## What to build

Two jobs that are deliberately **not** required checks, because they need infrastructure most contributors
do not have: harness conformance (the four Codex and OpenCode combinations, needing both harnesses
installed and authenticated) and provider conformance (GitHub.com and GitHub Enterprise operations,
needing credentials and a disposable repository). Both run on a schedule, by manual dispatch, and
automatically for a pull request touching an adapter path. An ordinary pull request triggers neither and is
never gated by them.

Each run writes rows into a published support matrix recording the combination, the tested harness and
tool versions, the observed Integration Capabilities, and the stated limitations. A run that cannot
execute records an explicit skip reason and leaves its rows unsupported: **a skip is never support.** Rows
carry the age of the run that produced them, and a row older than the configured threshold renders with its
age rather than as current. Publish the matrix into the reference documentation layer, regenerated by CI
rather than hand-edited.

Build the entry-point contract so that the jobs run and record honest skips before either suite exists.
`harness-adapters#07` owns the harness conformance suite entry point, its result shape, and the support
matrix row schema; provider conformance content is owned by `git-integration` and its entry point should be
agreed with that spec when its suite lands. **Ship this slice deliberately empty** — with both suites
absent, the workflow still completes and publishes a matrix in which every row is unsupported with a stated
reason. That is what makes the slice startable now instead of blocked behind two other specs, and it is an
accurate description of the current evidence.

The spec names where the risk sits: a job that does not block a merge will rot. Row age makes a stale claim
visibly stale rather than quietly wrong, and the adapter-path trigger catches the changes most likely to
break a row. What stays uncovered is a harness updating underneath a row that nothing touched, which is
what the scheduled run exists for.

## Acceptance criteria

- [ ] A scheduled run, a manual dispatch, and a pull request touching an adapter path each trigger the correct job; an ordinary pull request triggers neither and is not gated by them.
- [ ] A run with the harness absent records a skip reason and leaves the affected rows unsupported — no row moves to supported.
- [ ] The matrix lists the four harness combinations separately and each provider deployment separately; a native-only pass leaves cross-harness rows unsupported.
- [ ] Each row carries the timestamp and versions of the run that produced it, and a row older than the configured threshold renders with its age rather than as current.
- [ ] The published matrix is reachable from the reference documentation and is regenerated by CI rather than hand-edited.
- [ ] With both suites absent, the workflow still runs to completion and publishes a matrix in which every row is unsupported with a stated reason.

## Blocked by

- `release-engineering#01` — provides the workflow directory, the named CI scripts, and the documentation tree the matrix is published into.
