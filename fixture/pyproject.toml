[project]
name = "gantry-fixture-greeting"
version = "0.1.0"
description = "Minimal Python greeting project used as the Gantry reference fixture."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
